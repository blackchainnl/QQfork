# v30.1.3 signed corrective release authorization

## Incident and preserved record

The immutable annotated `v30.1.2` tag points to
`b3223d3230f15ba3a7c55376ff7d1f06265feb05`. Production run `29576545405`
stopped in the release-tool gate after two regression tests mutated tracked
fixture files without committing the changes. The canonical provenance
implementation correctly read exact Git blobs, not uncommitted working-tree
bytes. The failure occurred before package assembly, attestations, draft
creation, asset upload, or publication.

The tag, workflow run, and logs remain preserved. They are not moved, deleted,
relabeled, edited, or reused as v30.1.3 release assets. There is no v30.1.2
production release.

## One-time quick path

The Blackcoin-Dev operator authorized a one-time v30.1.3 quick path on
2026-07-17. The corrective test helper stages and commits each tracked fixture
mutation before checking that canonical Git-blob provenance detects source
drift. No executable, consensus, wallet, GUI, P2P, staking, mining, dependency,
or package-build recipe is changed. The release workflow changes only the
corrective gate orchestration, signing enforcement, and version metadata.

The workflow must fail closed if the candidate includes a path or object type
outside its explicit corrective allowlist. Historical successful jobs may be
recorded only as inherited evidence for unchanged code and must never be
described as v30.1.3-SHA test results. No prior package, checksum, SBOM,
provenance statement, or release asset may be relabeled or reused.

## Source identity and authorization

The protected production acknowledgement is exactly `V30.1.3`. It is an
internal release authorization value, not a cryptographic signature.

The exact source commit and annotated `v30.1.3` tag must both be SSH-signed by
Blackcoin-Dev and verified by GitHub. Publication must stop if either object is
unsigned, signed by another identity, points to another source commit, or is
not verified. The signing key is registered to the Blackcoin-Dev GitHub
account as a signing-only key. Its fingerprint is
`SHA256:jAkpBudDw+ntWHSUx3e1KY+czAFjnlaPxQtRFtptL70`. The private key and
passphrase must never enter the repository, workflow artifacts, logs, or
GitHub Actions secrets.

## Package disclosure

The signed Git objects authenticate the source commit and tag. They do not
code-sign the compiled packages. Windows binaries and installers remain
without Authenticode signatures. macOS applications use identity-free ad-hoc
launch signatures and are not Developer-ID signed or notarized. Release
metadata must distinguish the signed source identity from the platform-
unsigned packages and must not describe package files as signed.

## Completion

Publication requires fresh v30.1.3 packages bound to the exact signed source
commit, package checksums and metadata, the required provenance records, and a
successful immutable GitHub release assembly. The exact workflow controls are
authoritative. The quick path does not rerun duplicate-builder reproducibility
on the v30.1.3 SHA; historical reproducibility evidence is labeled separately
and must not be represented as current-source evidence. Package builds and
structural package checks remain fresh. If a mandatory v30.1.3-bound check
fails, publication stops and the protected tag is not moved, deleted, or
recreated.

The next roadmap release returns to the complete current-SHA validation gate.
This one-time exception cannot be inferred for another version, source, tag,
or run.
